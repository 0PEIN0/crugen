from django.utils.translation import ugettext_lazy as _

from rest.serializers import BaseModelSerializer

from ...models import {{model_name}}


class {{model_name}}CreateUpdateSerializer(BaseModelSerializer):

{{custom_fields}}

    class Meta:
        model = {{model_name}}
        fields = ({{fields}})
